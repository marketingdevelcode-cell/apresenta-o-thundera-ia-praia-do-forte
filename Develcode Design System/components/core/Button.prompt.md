Primary action control — always a full pill, never a rounded rectangle.

```jsx
<Button variant="primary" size="md">Falar com o time</Button>
```

Use `onDark` on near-black pages (glowing blue outline), `inverse` on filled blue panels. One primary per view.
