Headline proof number — use in rows of three or four, never alone.

```jsx
<MetricTile value="+80%" label="NPS de Satisfação" />
```

Tones: `blue` (default, filled gradient), `dark`, `light`. Keep labels to two short lines; write growth numbers with a leading `+`.
